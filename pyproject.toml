[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "playlists"
version = "0.1.0"
description = "Read and write M3U/M3U8 audio playlists"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "ryoung912" }]
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
